# Why do we need `Comparable` / `Comparator`?
- Whenever we want to **compare and sort Java objects** according to ascending or descending order, we use either **`Comparable`** or **`Comparator`**.
- To compare the objects, we need to define **how one object should be compared with another** using either **`Comparable`** or **`Comparator`** interface.