# 1. `print()`
`print()` is used to display output **on the same line** and it **does not move the cursor to the next line** after printing.
# 2.`println()`
`println()` is used to display output and it **moves the cursor to the next line** after printing, so the next output comes on a new line.

[**PTO**](70-VARIABLES.md)